import 'package:fluent_ui/fluent_ui.dart';

class AppColors {
  // Primary Colors
  static const accentLight = Color(0xff2563EB); // Classic Blue (Light Mode)
  static const accentDark = Color(0xff1E40AF); // Darker Blue (Dark Mode)

  // Background Colors
  static const backgroundLight = Color(0xffF9FAFB); // Soft White (Light Mode)
  static const backgroundDark = Color(0xff1F2937); // Dark Gray (Dark Mode)

  // Surface/Card Colors
  static const cardLight = Color(0xFFFFFFFF); // Pure White (Light Mode)
  static const cardDark = Color(0xFF374151); // Darker Gray (Dark Mode)

  // Text Colors
  static const textLight = Color(0xff111827); // Dark Gray (Light Mode)
  static const textDark = Color(0xffF9FAFB); // Soft White (Dark Mode)
}
